# 🛠 Подробная инструкция по установке и настройке Gazeta

Пошаговая инструкция по установке и настройке системы Gazeta.

## 📋 Содержание

1. [Системные требования](#системные-требования)
2. [Получение API credentials](#получение-api-credentials)
3. [Установка](#установка)
4. [Конфигурация](#конфигурация)
5. [Первый запуск](#первый-запуск)
6. [Автоматизация](#автоматизация)
7. [Продвинутая настройка](#продвинутая-настройка)

## Системные требования

### Минимальные требования

- **OS**: Linux, macOS, или Windows
- **Python**: 3.8 или выше
- **RAM**: 512 MB (рекомендуется 1 GB+)
- **Диск**: 100 MB для кода + место для базы данных
- **Интернет**: Стабильное подключение для Telegram API

### Проверка версии Python

```bash
python3 --version
# или
python --version
```

Должна быть версия 3.8 или выше.

## Получение API credentials

### 1. Telegram API credentials

1. Откройте https://my.telegram.org
2. Войдите с вашим номером телефона
3. Перейдите в "API development tools"
4. Создайте новое приложение:
   - App title: `Gazeta` (или любое другое название)
   - Short name: `gazeta`
   - Platform: `Desktop`
   - Description: (опционально)
5. Сохраните:
   - `api_id` (число, например `12345678`)
   - `api_hash` (строка, например `abc123def456...`)

**Важно**: Никому не передавайте эти данные!

### 2. BotHub API key

1. Откройте https://bothub.chat
2. Зарегистрируйтесь или войдите
3. Перейдите в настройки профиля → API Keys
4. Создайте новый ключ
5. Скопируйте API ключ (начинается с `sk-...`)
6. Пополните баланс для использования API

**Цены**: BotHub использует OpenAI-совместимые модели. Рекомендуемая модель `gpt-4o-mini` - самая дешевая.

### 3. ID чата для отправки отчетов

Узнать ID чата можно несколькими способами:

**Способ 1**: Использовать @userinfobot
1. Напишите боту @userinfobot в Telegram
2. Для приватного чата: переслайте любое сообщение из чата боту
3. Для канала: добавьте бота в канал и он покажет ID

**Способ 2**: Использовать @getidsbot
1. Напишите @getidsbot
2. Следуйте инструкциям

**Формат ID**:
- Личный чат: положительное число (например `123456789`)
- Группа/канал: отрицательное число (например `-1001234567890`)

## Установка

### Шаг 1: Клонирование репозитория

```bash
# Через Git
git clone <repository-url>
cd gazeta

# Или скачать архив и распаковать
```

### Шаг 2: Создание виртуального окружения

**Linux/macOS:**
```bash
python3 -m venv venv
source venv/bin/activate
```

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

После активации в начале строки появится `(venv)`.

### Шаг 3: Установка зависимостей

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

Должно установиться примерно 20-30 пакетов.

### Шаг 4: Создание конфигурационного файла

```bash
cp .env.example .env
```

**Windows:**
```bash
copy .env.example .env
```

## Конфигурация

Откройте файл `.env` в текстовом редакторе:

```bash
nano .env
# или
vim .env
# или откройте в любом редакторе
```

### Обязательные параметры

```bash
# Telegram API (из шага "Получение API credentials")
TELEGRAM_API_ID=12345678
TELEGRAM_API_HASH=abc123def456ghijk789
TELEGRAM_PHONE=+79001234567

# BotHub API
BOTHUB_API_KEY=sk-proj-abc123...

# ID чата для отчетов
REPORT_CHAT_ID=-1001234567890

# Каналы и чаты для мониторинга
CHANNELS=@tech_news:channel,@design_chat:chat
```

### Формат CHANNELS

Формат: `идентификатор:тип`

**Типы**:
- `channel` - канал (только посты, без повестки дня)
- `chat` - чат (все сообщения + генерация повестки дня)

**Идентификаторы**:
- Username с @: `@tech_news`
- Числовой ID: `-1001234567890`

**Примеры**:
```bash
# Публичный канал
CHANNELS=@durov:channel

# Несколько источников
CHANNELS=@tech_news:channel,@design_chat:chat,@python_ru:chat

# Приватные каналы (по ID)
CHANNELS=-1001234567890:channel,-1009876543210:chat

# Смешанный формат
CHANNELS=@public_channel:channel,-1001234567890:chat,@another_chat:chat
```

### Опциональные параметры

```bash
# База данных (по умолчанию: data/gazeta.db)
DATABASE_PATH=data/gazeta.db

# Timezone (по умолчанию: Europe/Moscow)
TIMEZONE=Europe/Moscow

# Заголовок отчета
REPORT_TITLE=📰 Дневная сводка

# Логирование
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR
LOG_FILE=logs/gazeta.log
```

### Сохранение

После заполнения всех параметров сохраните файл:
- **nano**: Ctrl+O, Enter, Ctrl+X
- **vim**: Esc, :wq, Enter

## Первый запуск

### 1. Проверка подключений

```bash
python main.py test
```

**Первый запуск Telegram**: При первом подключении Telegram попросит:
1. Код подтверждения (придет в Telegram на ваш телефон)
2. Возможно, 2FA пароль (если включен)

Введите эти данные в терминал.

**Ожидаемый результат**:
```
✅ Telegram: Подключено
✅ Тест получения данных: Tech News
✅ BotHub API: Доступно
```

### 2. Тестовый сбор (без отправки)

```bash
python main.py run --no-send
```

Система соберет сообщения за вчерашний день и покажет предпросмотр отчета в логах.

**Что происходит**:
1. Подключение к Telegram
2. Сбор сообщений из всех указанных каналов/чатов
3. Генерация повестки дня для чатов (через BotHub)
4. Формирование отчета
5. Сохранение в базу данных
6. Показ предпросмотра (первые 500 символов)

### 3. Реальный запуск (с отправкой)

```bash
python main.py run
```

Отчет будет отправлен в указанный чат.

### 4. Проверка статистики

```bash
python main.py stats
```

Покажет количество источников, сообщений и отчетов в базе данных.

## Автоматизация

### Вариант 1: Cron (Linux/macOS)

#### Настройка cron job

```bash
# Открыть crontab
crontab -e
```

#### Добавить задачу

Для запуска каждый день в 9:00:

```bash
0 9 * * * cd /full/path/to/gazeta && /full/path/to/gazeta/venv/bin/python main.py run >> /full/path/to/gazeta/logs/cron.log 2>&1
```

**Замените `/full/path/to/gazeta`** на реальный путь к проекту!

Узнать путь:
```bash
cd /path/to/gazeta
pwd
```

#### Другие варианты расписания

```bash
# Каждый день в 9:00
0 9 * * * ...

# Два раза в день (9:00 и 18:00)
0 9,18 * * * ...

# По рабочим дням в 9:00 (пн-пт)
0 9 * * 1-5 ...

# Каждый час
0 * * * * ...
```

#### Проверка cron job

```bash
# Список активных задач
crontab -l

# Логи cron
tail -f /full/path/to/gazeta/logs/cron.log
```

### Вариант 2: Systemd Timer (Linux)

#### Создать service файл

`/etc/systemd/system/gazeta.service`:

```ini
[Unit]
Description=Gazeta Telegram Digest
After=network.target

[Service]
Type=oneshot
User=your_username
WorkingDirectory=/full/path/to/gazeta
ExecStart=/full/path/to/gazeta/venv/bin/python main.py run
StandardOutput=append:/full/path/to/gazeta/logs/systemd.log
StandardError=append:/full/path/to/gazeta/logs/systemd.log
```

#### Создать timer файл

`/etc/systemd/system/gazeta.timer`:

```ini
[Unit]
Description=Gazeta Daily Timer

[Timer]
OnCalendar=daily
OnCalendar=09:00
Persistent=true

[Install]
WantedBy=timers.target
```

#### Активировать

```bash
sudo systemctl daemon-reload
sudo systemctl enable gazeta.timer
sudo systemctl start gazeta.timer

# Проверить статус
sudo systemctl status gazeta.timer
sudo systemctl list-timers
```

### Вариант 3: Windows Task Scheduler

1. Откройте "Task Scheduler"
2. Create Basic Task
3. Name: `Gazeta Daily Digest`
4. Trigger: Daily, 09:00
5. Action: Start a program
   - Program: `C:\path\to\gazeta\venv\Scripts\python.exe`
   - Arguments: `main.py run`
   - Start in: `C:\path\to\gazeta`
6. Finish

## Продвинутая настройка

### Изменение количества тем в повестке дня

В файле `main.py` найдите строку:

```python
digest = await digest_gen.generate_digest_for_chat(
    identifier, yesterday_start, yesterday_end, max_topics=7  # ← изменить здесь
)
```

Измените `max_topics` на нужное значение (рекомендуется 5-10).

### Использование другой модели BotHub

В файле `src/bothub_client.py` в методе `generate_digest`:

```python
async def generate_digest(
    self,
    messages: List[str],
    model: str = "openai/gpt-4o-mini",  # ← изменить здесь
    max_topics: int = 7,
) -> Optional[str]:
```

Доступные модели (см. BotHub documentation):
- `openai/gpt-4o-mini` (рекомендуется, дешево)
- `openai/gpt-4o`
- `openai/gpt-4-turbo`
- `anthropic/claude-3.5-sonnet`

### Настройка лимита сообщений для анализа

В файле `src/digest_generator.py`:

```python
max_messages = 200  # ← изменить здесь
```

Увеличение лимита:
- ✅ Более точный анализ
- ❌ Больше затрат на API
- ❌ Медленнее работает

### Логирование в несколько файлов

Измените `src/config.py` для ротации логов:

```python
from logging.handlers import RotatingFileHandler

handler = RotatingFileHandler(
    config.LOG_FILE,
    maxBytes=10*1024*1024,  # 10 MB
    backupCount=5
)
```

## Troubleshooting

### Проблема: "No module named 'src'"

**Решение**:
```bash
# Убедитесь, что запускаете из корня проекта
cd /path/to/gazeta
python main.py run
```

### Проблема: "Database is locked"

**Причина**: Две копии программы запущены одновременно.

**Решение**:
```bash
# Найти процессы
ps aux | grep "main.py"

# Убить процесс
kill <PID>
```

### Проблема: BotHub возвращает ошибку 401

**Причина**: Неверный API ключ.

**Решение**:
1. Проверьте API ключ в `.env`
2. Создайте новый ключ на bothub.chat
3. Убедитесь, что ключ скопирован полностью

### Проблема: Telegram "Session file is corrupted"

**Решение**:
```bash
# Удалить session файл
rm gazeta_session.session

# Запустить снова (попросит авторизоваться заново)
python main.py test
```

### Проблема: Нет доступа к приватному каналу

**Причина**: Ваш аккаунт не имеет доступа к каналу.

**Решение**:
1. Убедитесь, что вы подписаны на канал/состоите в чате
2. Для приватных каналов используйте числовой ID вместо username
3. Проверьте ID через @userinfobot

## Обновление

### Обновление кода

```bash
cd /path/to/gazeta
git pull

# Обновить зависимости (если изменились)
source venv/bin/activate  # или venv\Scripts\activate на Windows
pip install -r requirements.txt --upgrade
```

### Миграция базы данных

При обновлении схема БД обновляется автоматически при первом запуске.

Для ручной миграции:
```bash
# Бэкап старой БД
cp data/gazeta.db data/gazeta.db.backup

# Запустить программу (схема обновится)
python main.py stats
```

## Безопасность

### Рекомендации

1. **Не коммитьте .env файл** в git
2. **Не давайте доступ** к session файлам
3. **Используйте приватные репозитории** для хранения конфигурации
4. **Регулярно меняйте** API ключи
5. **Ограничьте доступ** к серверу, где запущена система

### Ограничение прав пользователя

```bash
# Создать отдельного пользователя для gazeta
sudo useradd -m -s /bin/bash gazeta
sudo -u gazeta bash

# Установить проект под этим пользователем
```

## Поддержка

Если возникли проблемы:

1. Проверьте логи: `tail -f logs/gazeta.log`
2. Запустите с DEBUG: `python main.py --log-level DEBUG run`
3. Проверьте Issues в репозитории
4. Создайте новый Issue с описанием проблемы и логами

---

✅ После выполнения всех шагов система готова к работе!
